# Electron 侧栏折叠范围修复意图

## 问题

主导航改造成“主会话列 + 可选二级菜单列”后，沿用了旧的
`isSecondMenuCollapsed` 状态。NuwaClaw 顶部工具栏点击“收起二级菜单”时，主会话列
也被设置为宽度 0，导致 Logo、导航、收藏/置顶会话和底栏一起消失。

## 目标

- Electron 沉浸式环境中，主会话列和 Logo 区始终显示。
- NuwaClaw 工具栏只折叠当前业务域的二级菜单列。
- 浏览器环境继续保留原有整栏折叠交互。
- 不改变宿主 IPC 协议和既有状态回推机制。
